"use client";import { CartesianGrid,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";import type { CarbonEntry } from "@/types/carbon";
export function WeeklyTrend({entries}:{entries:CarbonEntry[]}){const data=Array.from({length:7},(_,i)=>{const d=new Date();d.setDate(d.getDate()-6+i);const key=d.toISOString().slice(0,10);return {day:d.toLocaleDateString("en",{weekday:"short"}),kg:entries.filter(e=>e.entry_date===key).reduce((s,e)=>s+Math.max(0,e.co2_amount),0)}});return <div className="h-64 w-full" aria-label="Weekly carbon emissions line chart"><ResponsiveContainer><LineChart data={data} margin={{top:8,right:8,left:-22,bottom:0}}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#dce4dc"/><XAxis dataKey="day" axisLine={false} tickLine={false} tick={{fontSize:12,fill:"#6b8178"}}/><YAxis axisLine={false} tickLine={false} tick={{fontSize:12,fill:"#6b8178"}}/><Tooltip contentStyle={{borderRadius:6,border:"1px solid #dce4dc"}} formatter={(v)=>[`${Number(v).toFixed(1)} kg`,`CO₂e`]}/><Line type="monotone" dataKey="kg" stroke="#1f7a5a" strokeWidth={3} dot={{fill:"#b8e34b",stroke:"#174c3c",strokeWidth:2,r:4}}/></LineChart></ResponsiveContainer></div>}
