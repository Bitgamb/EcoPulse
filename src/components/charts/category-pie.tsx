"use client";import { Cell,Pie,PieChart,ResponsiveContainer,Tooltip } from "recharts";import type { CarbonEntry,Category } from "@/types/carbon";import { pretty } from "@/lib/utils";
const colors=["#1f7a5a","#ef795f","#e2b93b","#5577a8","#8abf48"];
export function CategoryPie({entries}:{entries:CarbonEntry[]}){const totals=entries.reduce((a,e)=>(a[e.category]=(a[e.category]||0)+Math.max(0,e.co2_amount),a),{} as Partial<Record<Category,number>>);const data=Object.entries(totals).map(([name,value])=>({name:pretty(name),value}));return <div className="grid grid-cols-[1fr_120px] items-center"><div className="h-52"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" innerRadius={52} outerRadius={78} paddingAngle={3}>{data.map((_,i)=><Cell key={i} fill={colors[i%colors.length]}/>)}</Pie><Tooltip formatter={v=>`${Number(v).toFixed(1)} kg`}/></PieChart></ResponsiveContainer></div><div className="space-y-2">{data.map((x,i)=><div key={x.name} className="flex items-center gap-2 text-xs"><span className="size-2.5 rounded-sm" style={{background:colors[i]}}/><span className="text-ink/65">{x.name}</span></div>)}</div></div>}
