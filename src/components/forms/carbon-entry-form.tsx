"use client";import { useMemo,useState } from "react";import { useRouter } from "next/navigation";import { Calculator,CheckCircle2 } from "lucide-react";import { activities,calculateCO2,getActivityMeta } from "@/lib/emission-factors";import type { Category } from "@/types/carbon";import { useEcoData } from "@/components/dashboard/data-provider";import { Button } from "@/components/ui/button";import { entrySchema } from "@/lib/validations";import { pretty } from "@/lib/utils";
export function CarbonEntryForm(){const [category,setCategory]=useState<Category>("transport"),[type,setType]=useState("car_km"),[value,setValue]=useState(10),[message,setMessage]=useState("");const {addEntry}=useEcoData(),router=useRouter();const meta=getActivityMeta(type),estimate=useMemo(()=>calculateCO2(type,value||0),[type,value]);function changeCategory(c:Category){setCategory(c);setType(Object.keys(activities[c])[0])}function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const data={category,activity_type:type,value,unit:String(meta?.[2]||"unit"),entry_date:fd.get("entry_date"),note:fd.get("note")};const parsed=entrySchema.safeParse(data);if(!parsed.success){setMessage(parsed.error.issues[0].message);return}addEntry({id:crypto.randomUUID(),...parsed.data,category:parsed.data.category,co2_amount:estimate});setMessage("Activity added. Your dashboard is up to date.");setTimeout(()=>router.push("/dashboard"),700)}return <form onSubmit={submit} className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><div className="rounded-lg border border-ink/10 bg-white p-5 shadow-sm sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><div><label htmlFor="category">Category</label><select id="category" value={category} onChange={e=>changeCategory(e.target.value as Category)}>{Object.keys(activities).map(c=><option key={c} value={c}>{pretty(c)}</option>)}</select></div><div><label htmlFor="activity">Activity type</label><select id="activity" value={type} onChange={e=>setType(e.target.value)}>{Object.entries(activities[category]).map(([key,v])=><option key={key} value={key}>{v[0]}</option>)}</select></div><div><label htmlFor="value">Amount</label><div className="relative"><input id="value" type="number" min="0.01" step="0.01" required value={value} onChange={e=>setValue(Number(e.target.value))}/><span className="absolute right-3 top-3 text-sm text-ink/45">{meta?.[2]}</span></div></div><div><label htmlFor="entry_date">Date</label><input id="entry_date" name="entry_date" type="date" required defaultValue={new Date().toISOString().slice(0,10)} max={new Date().toISOString().slice(0,10)}/></div><div className="sm:col-span-2"><label htmlFor="note">Note <span className="font-normal text-ink/40">(optional)</span></label><textarea id="note" name="note" rows={4} maxLength={300} placeholder="Anything useful to remember about this activity"/></div></div>{message&&<p role="status" className="mt-5 flex items-center gap-2 rounded-md bg-emerald/10 p-3 text-sm font-bold text-emerald"><CheckCircle2 size={18}/>{message}</p>}<Button className="mt-6 w-full sm:w-auto" type="submit">Add to my footprint</Button></div><aside className="space-y-4"><div className="rounded-lg bg-ink p-6 text-white"><div className="flex items-center gap-2 text-lime"><Calculator size={18}/><span className="text-xs font-extrabold uppercase">Live estimate</span></div><p className={`mt-8 font-display text-5xl ${estimate<0?"text-lime":""}`}>{estimate.toFixed(2)}</p><p className="text-sm text-white/55">kg CO₂e</p><div className="mt-8 border-t border-white/15 pt-4 text-sm text-white/65"><div className="flex justify-between"><span>Factor</span><span>{meta?.[1]} kg / {String(meta?.[2]).replace(/s$/,"")}</span></div><div className="mt-2 flex justify-between"><span>Impact level</span><span className="font-bold text-lime">{estimate<1?"Low":estimate<5?"Moderate":"High"}</span></div></div>{estimate<0&&<p className="mt-4 text-xs text-lime">This activity represents avoided emissions and counts toward CO₂ saved.</p>}</div><div className="rounded-lg border border-ink/10 bg-white p-5"><p className="text-sm font-bold">How estimates work</p><p className="mt-2 text-xs leading-5 text-ink/55">We multiply your activity by a documented average emission factor. Results are estimates designed to help you compare habits, not an audited carbon inventory.</p></div></aside></form>}
